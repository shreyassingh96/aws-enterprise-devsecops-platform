apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: devsecops-apps
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: frontend
